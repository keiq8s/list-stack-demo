# Алгоритмы сортировки и поиска

Комплексная реализация классических алгоритмов сортировки и поиска на Python, Java и C++ с детальным анализом и примерами работы.

## 📋 Содержание
- [Алгоритмы сортировки](#алгоритмы-сортировки)
- [Алгоритмы поиска](#алгоритмы-поиска)
- [Сравнительные таблицы](#сравнительные-таблицы)
- [Примеры запуска](#примеры-запуска)
- [Рекомендации по выбору](#рекомендации-по-выбору)

---

## 🔄 Алгоритмы сортировки

### 1. Selection Sort (Сортировка выбором)

**Принцип работы:**
- На каждой итерации находим минимальный элемент в неотсортированной части
- Меняем его местами с первым элементом неотсортированной части
- Перемещаем границу отсортированной части на одну позицию вправо

**Особенности синтаксиса:**
- **Python:** `arr[i], arr[min_idx] = arr[min_idx], arr[i]` — множественное присваивание
- **Java:** требуется временная переменная для обмена значений
- **C++:** можно использовать `std::swap()` или временную переменную

**Сложность:** O(n²) во всех случаях — всегда проходим по всему массиву

---

### 2. Bubble Sort (Сортировка пузырьком)

**Принцип работы:**
- Сравниваем соседние элементы
- Если они в неправильном порядке — меняем местами
- За каждый проход самый большой элемент "всплывает" в конец
- Оптимизация: если за проход не было обменов — массив отсортирован

**Особенности синтаксиса:**
- **Python:** флаг `swapped = False` с проверкой `if not swapped: break`
- **Java:** `boolean swapped` с `if (!swapped) break;`
- **C++:** `bool swapped` с той же логикой

**Сложность:** 
- Лучший случай: O(n) — массив уже отсортирован
- Худший случай: O(n²) — массив отсортирован в обратном порядке

---

### 3. Insertion Sort (Сортировка вставками)

**Принцип работы:**
- Делим массив на отсортированную и неотсортированную части
- Берем элемент из неотсортированной части
- Вставляем его на правильное место в отсортированную часть
- Сдвигаем большие элементы вправо

**Особенности синтаксиса:**
- **Python:** `while j >= 0 and arr[j] > key` — условие с коротким замыканием
- **Java/C++:** аналогичная логика с `&&` оператором
- Важно: проверяем `j >= 0` ПЕРЕД обращением к `arr[j]`

**Сложность:**
- Лучший случай: O(n) — массив уже отсортирован
- Худший случай: O(n²) — массив отсортирован в обратном порядке

---

### 4. Merge Sort (Сортировка слиянием)

**Принцип работы:**
- **Разделяй:** делим массив пополам рекурсивно до элементов
- **Властвуй:** сортируем подмассивы (базовый случай — массив из 1 элемента)
- **Объединяй:** сливаем два отсортированных массива в один

**Особенности синтаксиса:**
- **Python:** срезы `arr[:mid]` и `arr[mid:]`, list comprehensions
- **Java:** `Arrays.copyOfRange()` для создания подмассивов
- **C++:** векторы с конструктором `vector<int>(arr.begin(), arr.begin() + mid)`

**Сложность:** O(n log n) во всех случаях, память O(n) для временных массивов

---

### 5. Shell Sort (Сортировка Шелла)

**Принцип работы:**
- Улучшенная версия сортировки вставками
- Сравниваем и обмениваем элементы на расстоянии `gap`
- Постепенно уменьшаем `gap` (обычно делим на 2)
- Когда `gap = 1`, выполняется обычная сортировка вставками

**Особенности синтаксиса:**
- **Python/Java/C++:** похожая логика с циклами
- Ключевое: `gap //= 2` (Python), `gap /= 2` (Java/C++)
- Внутренний цикл похож на insertion sort с шагом `gap`

**Сложность:** зависит от последовательности gap, обычно O(n log² n)

---

### 6. Quick Sort (Быстрая сортировка)

**Принцип работы:**
- Выбираем опорный элемент (pivot)
- Разделяем массив: элементы < pivot, = pivot, > pivot
- Рекурсивно сортируем левую и правую части
- Объединяем результат

**Особенности синтаксиса:**
- **Python:** list comprehensions `[x for x in arr if x < pivot]` — элегантно и кратко
- **Java/C++:** требуется ручное разделение с дополнительными массивами или in-place
- Выбор pivot: средний элемент `arr[len(arr) // 2]`

**Сложность:**
- Средний случай: O(n log n)
- Худший случай: O(n²) — неудачный выбор pivot

---

### 7. Heap Sort (Пирамидальная сортировка)

**Принцип работы:**
- Строим max-heap (родитель ≥ детей)
- Извлекаем максимум (корень) в конец массива
- Восстанавливаем heap для оставшейся части
- Повторяем до полной сортировки

**Особенности синтаксиса:**
- **Python/Java/C++:** индексация детей: `left = 2*i + 1`, `right = 2*i + 2`
- Функция `heapify()` рекурсивно поддерживает свойство heap
- Строим heap снизу вверх: с `n//2 - 1` до `0`

**Сложность:** O(n log n) во всех случаях, память O(1)

---

## 🔍 Алгоритмы поиска

### 1. Linear Search (Линейный поиск)

**Принцип работы:**
- Последовательно проверяем каждый элемент
- Возвращаем индекс при нахождении
- Возвращаем -1, если не найдено

**Особенности синтаксиса:**
- **Python:** `for i in range(len(arr))`
- **Java:** `for (int i = 0; i < arr.length; i++)`
- **C++:** `for (int i = 0; i < arr.size(); i++)`

**Сложность:** O(n), не требует сортировки

---

### 2. Binary Search (Бинарный поиск)

**Принцип работы:**
- Требует отсортированный массив
- Сравниваем с элементом в середине
- Отбрасываем половину массива
- Повторяем для оставшейся части

**Особенности синтаксиса:**
- Вычисление середины: `mid = left + (right - left) // 2` — избегаем переполнения
- **Python:** `//` — целочисленное деление
- **Java/C++:** автоматическое целочисленное деление

**Сложность:** O(log n)

---

### 3. Interpolation Search (Интерполяционный поиск)

**Принцип работы:**
- Улучшенный binary search для равномерно распределенных данных
- Предполагаем позицию на основе значения
- Формула: `pos = low + (target - arr[low]) * (high - low) / (arr[high] - arr[low])`

**Особенности синтаксиса:**
- **Python:** `//` для целочисленного деления
- **Java/C++:** нужна явная типизация для избежания переполнения
- Важно: проверка границ `pos < low or pos > high`

**Сложность:**
- Средний случай: O(log log n)
- Худший случай: O(n) — неравномерное распределение

---

### 4. Fibonacci Search (Поиск Фибоначчи)

**Принцип работы:**
- Делит массив используя числа Фибоначчи
- Находим наименьшее число Фибоначчи ≥ n
- Исключаем диапазоны на основе чисел Фибоначчи

**Особенности синтаксиса:**
- Три переменные: `fib_m2`, `fib_m1`, `fib_m` (последовательные числа Фибоначчи)
- **Python:** `min(offset + fib_m2, n - 1)` для безопасной индексации
- Смещение `offset` отслеживает исключенный диапазон

**Сложность:** O(log n), меньше операций деления чем binary search

---

## 📊 Сравнительные таблицы

### Сортировка

| Алгоритм | Лучший случай | Средний случай | Худший случай | Память | Стабильность |
|----------|---------------|----------------|---------------|--------|--------------|
| **Selection Sort** | O(n²) | O(n²) | O(n²) | O(1) | ❌ Нет |
| **Bubble Sort** | O(n) | O(n²) | O(n²) | O(1) | ✅ Да |
| **Insertion Sort** | O(n) | O(n²) | O(n²) | O(1) | ✅ Да |
| **Merge Sort** | O(n log n) | O(n log n) | O(n log n) | O(n) | ✅ Да |
| **Shell Sort** | O(n log n) | O(n^1.5) | O(n²) | O(1) | ❌ Нет |
| **Quick Sort** | O(n log n) | O(n log n) | O(n²) | O(log n) | ❌ Нет |
| **Heap Sort** | O(n log n) | O(n log n) | O(n log n) | O(1) | ❌ Нет |

### Поиск

| Алгоритм | Лучший случай | Средний случай | Худший случай | Память | Требует сортировки |
|----------|---------------|----------------|---------------|--------|--------------------|
| **Linear Search** | O(1) | O(n) | O(n) | O(1) | ❌ Нет |
| **Binary Search** | O(1) | O(log n) | O(log n) | O(1) | ✅ Да |
| **Interpolation Search** | O(1) | O(log log n) | O(n) | O(1) | ✅ Да (равномерное) |
| **Fibonacci Search** | O(1) | O(log n) | O(log n) | O(1) | ✅ Да |

---

## 🚀 Примеры запуска

### Python
```bash
python sort_search.py
```

### Java
```bash
javac SortSearch.java
java SortSearch
```

### C++
```bash
g++ -std=c++11 sort_search.cpp -o sort_search
./sort_search
```

---

## 💡 Рекомендации по выбору алгоритма

### Сортировка

#### Для малых массивов (n < 50)
**→ Insertion Sort**
- ✅ Простая реализация
- ✅ Эффективна на малых данных
- ✅ Хорошо работает с почти отсортированными данными

#### Для средних и больших массивов

**→ Quick Sort** (общий случай)
- ✅ Самая быстрая в среднем
- ✅ Минимальное использование памяти
- ⚠️ Нестабильная, худший случай O(n²)

**→ Merge Sort** (нужна надежность)
- ✅ Гарантированная производительность O(n log n)
- ✅ Стабильная сортировка
- ⚠️ Требует O(n) дополнительной памяти

**→ Heap Sort** (ограничена память)
- ✅ Гарантированная производительность O(n log n)
- ✅ O(1) дополнительной памяти
- ⚠️ Нестабильная, медленнее Quick Sort на практике

#### Специальные случаи

**Почти отсортированные данные** → Insertion Sort O(n)

**Нужна стабильность** → Merge Sort или Bubble Sort

**Ограниченная память** → Heap Sort или Shell Sort

**Много повторяющихся элементов** → Quick Sort с 3-way partition

---

### Поиск

#### Несортированные данные
**→ Linear Search** — единственный вариант

#### Отсортированные данные

**→ Binary Search** (универсальный выбор)
- ✅ Простая реализация
- ✅ Стабильная производительность O(log n)
- ✅ Минимальное использование памяти

**→ Interpolation Search** (равномерное распределение)
- ✅ O(log log n) для равномерных данных
- ⚠️ O(n) для неравномерных данных
- Подходит для числовых данных

**→ Fibonacci Search** (альтернатива Binary)
- ✅ Меньше операций деления
- ✅ Полезно на старых процессорах
- ⚠️ Сложнее в реализации

---

## 📈 Итоговые выводы

### Сортировка

1. **O(n²) алгоритмы** (Selection, Bubble, Insertion)
   - Просты в реализации и понимании
   - Эффективны только для малых данных (n < 50)
   - Insertion Sort — лучший выбор среди них

2. **O(n log n) алгоритмы** (Merge, Quick, Heap)
   - Стандарт для больших данных
   - Каждый имеет свои преимущества

3. **Merge Sort** — выбор когда:
   - Нужна предсказуемость
   - Требуется стабильность
   - Память не критична

4. **Quick Sort** — выбор когда:
   - Важна максимальная скорость
   - Стабильность не нужна
   - Есть хороший выбор pivot

5. **Heap Sort** — выбор когда:
   - Ограничена память
   - Нужна гарантия O(n log n)
   - Стабильность не нужна

### Поиск

1. **Linear Search** — простой, но медленный O(n)
   - Единственный вариант для несортированных данных

2. **Binary Search** — золотой стандарт для отсортированных данных
   - O(log n) производительность
   - Простая реализация

3. **Специализированные алгоритмы** (Interpolation, Fibonacci)
   - Могут быть быстрее в специфических случаях
   - Требуют дополнительных условий

### Общий вывод

**Выбор алгоритма зависит от:**
- Размера данных
- Требований к памяти
- Необходимости стабильности
- Характеристик входных данных
- Требований к надежности

**Для большинства случаев:**
- Сортировка: **Quick Sort** или **Merge Sort**
- Поиск: **Binary Search**

---

## 📚 Дополнительные ресурсы

- [Визуализация алгоритмов](https://visualgo.net/)
- [Big-O шпаргалка](https://www.bigocheatsheet.com/)
- [Сравнение алгоритмов сортировки](https://www.youtube.com/watch?v=kPRA0W1kECg)

---
